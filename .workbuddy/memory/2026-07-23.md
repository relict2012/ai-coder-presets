# 2026-07-23 工作日志

## 拆分 backend-admin Skill（Claude Code 格式优化）

- 原始 `skills/backend-admin/SKILL.md` 有 663 行 / 13,568 字符，内容过重
- 按 Claude Code skill 最佳实践拆分为 3 个文件：
  - `SKILL.md`（81 行 / 2,640 字符）— 核心规则、决策表、禁止行为、自检清单，每次激活加载
  - `references/base-dto-reference.md`（190 行）— 基础 DTO 完整定义与示例，按需加载
  - `references/crud-and-sqlsugar.md`（112 行）— CRUD 模板与 SqlSugar 查询规范，按需加载
- 每次加载 token 节省约 80%，总文件量也减少约 29%（去除了重复说明和冗余格式）
- SKILL.md 中通过 `@references/xxx.md` 语法指向参考文件

## 强化 Mapster 映射规则

- 在 SKILL.md 新增「Mapster 映射（强制）」独立段，包含三行速查表和强制约束说明
- 新建 `references/mapster-reference.md`（118 行），包含：
  - 基础用法（Adapt 创建 / Adapt 映射到已有对象）
  - 映射配置（TypeAdapterConfig 处理名称不一致）
  - 允许手动补充的场景（仅系统字段如 CreatedTime）
  - 反模式示例（3 个典型禁止写法）
  - 场景速查表
- 在禁止行为和自检清单中强调「禁止逐字段手写属性赋值」
- crud-and-sqlsugar.md 的映射段精简并指向 mapster-reference

## 修正审计字段错误 + 补充 Admin.NET AOP 说明

- 发现 SKILL 中误导性内容：之前允许手动赋值 CreatedTime 等，但 Admin.NET 审计字段由 AOP 自动填充
- SKILL.md 新增「审计字段（AOP 自动赋值，禁止手动）」段，列出 6 个审计字段及 AOP 行为
- 修正 SKILL.md Mapster 段：删除"允许补充 CreatedTime"，改为"仅属性名不一致或关联查询时补充"
- 禁止行为新增：❌ 手动赋值审计字段
- 自检清单新增第 5 条：审计字段是否未手动赋值
- mapster-reference.md：
  - 新增审计字段段（含正确/错误对比示例）
  - "允许手动补充"段删除 CreatedTime/CreateUserId 示例，只保留关联查询
  - 新增反模式「手动赋值审计字段」
- crud-and-sqlsugar.md 映射段：修正错误示例，明确审计字段 AOP 自动填充
- base-dto-reference.md 使用规则第 5 条：Input DTO 不含审计字段，Output DTO 可含用于展示

## 补充 Entity 基类体系 + 扩展审计字段范围

- 用户提供了完整的 Admin.NET EntityBase 继承体系（9 个基类）
- SKILL.md 新增「Entity 基类选择决策」段，9 行决策表对应 9 种基类
- SKILL.md 审计字段段从原来只列 6 个 EntityBase 字段，扩展为按来源基类分类：
  - EntityBase：6 个审计字段（AOP 在 Insert/Update 拦截中赋值）
  - EntityBaseDel：IsDelete（默认值 `= false`）+ DeleteTime（软删除操作时赋值，不在 Insert/Update AOP 范围）
  - EntityBaseOrg 系列：OrgId（机构数据权限 AOP）
  - 租户基类系列：TenantId（租户 AOP）
- **修正关键错误**：IsDelete/DeleteTime 不属于 Insert/Update AOP，机制是：
  - IsDelete 字段默认值 = false
  - DeleteTime 在软删除操作时赋值
  - IDeletedFilter 是查询过滤器（SELECT 自动加 WHERE IsDelete = false）
- SKILL.md 审计字段段拆分为两个表：AOP 赋值字段表 + 非 AOP 字段机制表
- 新建 `references/entity-base-reference.md`（175 行）：
  - 完整继承关系图
  - 9 个基类的字段定义与 SugarColumn 特性说明
  - 每个 AOP 行为的注释（如 IsOnlyIgnoreUpdate、IDeletedFilter、IOrgIdFilter、ITenantIdFilter）
  - 查询自动过滤说明（软删除/机构/租户基类的 SqlSugar 全局过滤器）
  - 选择规则和禁止行为
- mapster-reference.md 审计字段段同步扩展为按来源基类分类
- SKILL.md 禁止行为新增：Entity 子类重复定义基类字段
- 自检清单新增第 3 条：Entity 是否正确继承基类

## 创建 project-dev-review 技能（开发计划严格评审）

- 用户提供了完整的 Tech Lead 评审框架（8 维度输出结构），要求转化为 skill
- 创建用户级技能：`~/.workbuddy/skills/project-dev-review/`
- 文件结构：
  - `SKILL.md` — 评审原则、触发条件、工作流（Step 0-3）、评审纪律
  - `references/review_checklist.md` — 8 个维度的详细检查清单、常见红旗信号、判断标准
- 核心评审原则：不默认需求正确、文档未体现=未设计、主动发现问题、不为中立给正面评价、所有问题需原因+建议、结合业务场景不机械建议通用能力、数据设计以满足业务需求为主
- 输出结构：总体结论 / 需求与功能 / 技术方案 / 数据设计 / 开发计划 / 风险清单 / 改进建议 / 最终决策
- 通过 skill-creator 验证

## 技能迁移到项目目录 + 强化证据原则与禁止建议清单

- 技能从用户级 `~/.workbuddy/skills/` 迁移到项目级 `D:\APP\ai-coder-presets\skills\project-dev-review\`
- 用户新增两条核心规则，已写入 SKILL.md 和 review_checklist.md：
  1. **证据原则**：文档明确描述=已设计、文档未体现=不做假设、不脑补、所有结论必须可指出依据
  2. **禁止默认建议清单**：缓存、MQ、Redis、微服务、DDD、事件驱动、审计日志、操作日志、权限细化——默认不建议，只有业务场景确实需要时才提并说明理由
- review_checklist.md 顶部新增「证据原则」和「禁止默认建议清单」两个贯穿所有维度的约束段
- 维度 3（技术方案）和维度 4（数据设计）的检查项中补充了对禁止清单的引用
